language: cpp

sudo: required

install:
  - sudo apt-get install cmake
  - cmake --version

jobs:
  include:
    - os: linux
      compiler: gcc

script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build .
