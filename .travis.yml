language: cpp

sudo: required

install:
  - sudo apt-get install cmake clang-tidy
  - cmake --version

jobs:
  include:
    - os: linux
      compiler: gcc

script:
  - ./syntax_check.sh
  - mkdir build
  - cd build
  - cmake .. -DBUILD_DOC=OFF
  - cmake --build .
  - ./test_amaynet
