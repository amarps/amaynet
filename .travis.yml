language: cpp

sudo: required

install:
  - sudo apt-get install cmake

jobs:
  include:
    - os: linux
      compiler: gcc

script:
  - mkdir build
  - cmake -S . -B ./build
  - cmake --build ./build
